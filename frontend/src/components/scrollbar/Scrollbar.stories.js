import { Scrollbar } from './Scrollbar';

export default {
  title: 'Scrollbar',
  component: Scrollbar,
  tags: ['autodocs']
};

export const Base = {
  args: {
  },
};

export const Content = {
  args: {
    content: true
  },
};