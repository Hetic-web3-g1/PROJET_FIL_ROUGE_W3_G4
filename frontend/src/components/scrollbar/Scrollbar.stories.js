import { ScrollbarComp } from './Scrollbar';

export default {
  title: 'Scrollbar',
  component: ScrollbarComp,
  tags: ['autodocs']
};

export const Base = {
  args: {
  },
};

export const Content = {
  args: {
    content: true
  },
};