import { ModalCreateMasterclass } from './ModalCreateMasterclass';

export default {
  title: 'ModalCreateMasterclass',
  component: ModalCreateMasterclass,
  argTypes: {
  },
};

export const Default = {
    args: {
    },
};
