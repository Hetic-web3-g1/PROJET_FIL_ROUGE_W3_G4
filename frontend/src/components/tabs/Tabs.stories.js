import Tabs from "./Tabs";

export default {
  title: 'Tabs',
  component: Tabs
};

export const Base = {
  args: {
  },
};

export const ActiveTabByDefault = {
  args: {
    tabActiveByDefault: 'Partition',
  },
};